import { PlaceholderScreen } from '@/components/system/PlaceholderScreen';

export function JournalScreen() {
  return <PlaceholderScreen name="Journal" hint="Text + voice composer lands in M5." />;
}
