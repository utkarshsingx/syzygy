import { PlaceholderScreen } from '@/components/system/PlaceholderScreen';

export function SignInScreen() {
  return <PlaceholderScreen name="Sign in" hint="Email + Google sign-in arrives in M3." />;
}
