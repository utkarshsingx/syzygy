import { initializeApp } from 'firebase-admin/app';

initializeApp();

export { acceptInvite } from './acceptInvite';
export { onPartnerMessage } from './onPartnerMessage';
export { deleteUserData } from './deleteUserData';
